# A105 編集操作入口の最小検証

実施日: 2026-09-18。A105で承認された分離環境・空プロジェクトでの観測。E1の一部を確認した。保存成功、Undo/Redo実行、出力完了、編集復元の成立は未実証。

## 環境と範囲

AviUtl2 2.1.9の既取得公式app.zipを、新規の `C:/Users/raimu/AppData/Local/Temp/clipchannel-a105-editor-6f349375` へ展開。zipのSHA256は `B6A210007B7EAE02C34BEDD084A893C096C3F3329B6B43F09214B5E8D4A77C41`。同梱aviutl2.txtの説明に従い、起動前にexe隣へdataフォルダを作り、設定を検証領域へ分離した。今回は本体標準メニューだけを調べたため、追加プラグインは導入していない。プラグイン経由の出力入口は未確認。

WindowsのComputer Useによるアクセシビリティツリーと画面を観測。利用者の既存プロジェクト・素材は開かず、空のRootシーンで確認した。観測対象だけを抽出した `ui-evidence.json` を同領域へ保存した。Temp領域は将来の自動整理で失われる可能性がある。

## 観測結果

| 対象 | 実際に観測したこと | 未確認 |
| --- | --- | --- |
| 保存 | ファイルメニューの「プロジェクトを保存」を操作し「名前を付けて保存」ダイアログへ到達。ファイル名欄、保存・キャンセルを観測。Escapeで取り消し | ファイル保存・成功通知・保存した版の識別 |
| 履歴 | 編集メニューの「元に戻す Ctrl+Z」「やり直し Ctrl+Shift+Z」を観測。空プロジェクトでは無効 | 編集のUndo/Redo実行、外部手編集との履歴境界 |
| 出力 | ファイル出力の子メニューにAVI・PNG・JPEG・連番ファイル出力を観測 | MP4のプラグイン入口、開始・完了・中止判定 |

メニューで得た数値IDは今回の観測値であり、公式の安定APIとは扱わない。要素indexが無効になる操作失敗があり、新しい状態を取得してメニューを開き直すと観測・操作できた。保存ダイアログでは親ウィンドウとの対象不一致によるクリック拒否も発生し、キーによる取消しを用いた。したがって、入口の存在を安定したUI連携の実証と同一視しない。

終了時は可視の検証用アプリをAlt+F4で終了し、プロセス終了を確認。最初の非表示起動で残った、同じ検証領域を実行元とするウィンドウなしの所有プロセスだけをPID・実行パスで確認して停止した。保存済みプロジェクトや出力動画は作成していない。

## 判定と次の残件

E1は本体標準の操作入口を部分確認。E2〜E5は未実施。今回、初版要件を変更する根拠は得ていない。次の優先課題は、専用プラグインとホスト操作を組み合わせた保存成功・履歴・排他・未保存状態の復元の成立確認。入口が見えることだけではA85の全編集要件を実現できるとは結論しない。

これは通常経路の観測であり、故意の失敗試験は注入していない。既存の模擬失敗3件やE5の復元試験は消化していない。本体機能実装・仕様確定・Issue分割は行っていない。
