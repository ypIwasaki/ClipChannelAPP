# 編集トランザクションの追加検証（2026-09-18）

仕様検討用の隔離プローブによる検証。仕様確定・製品実装は行っていない。前回の [境界検証](editor-boundary-validation.md) を補足する。3項目を試験したが、製品の排他・復元ゲート全体は未完了。

|対象|今回確認できたこと|残る制約|
|---|---|---|
|手編集排他|ホスト所有のモーダル待機画面中、ドラッグとUndoを受け付けず、解除後の持ち越し変更も観測しなかった|別ウィンドウ・別プラグイン等の全変更経路は未検証。復元まで連続した保護も未成立|
|保存→出力|保存先ファイルの識別値から対応する保存時状態を選べた。出力開始時に照合不能なら停止した|出力開始時はSDKの状態取得がUNAVAILABLE。正常な保存状態でも停止し、この経路の実用成立は未確認|
|生成・削除の復元|画像生成＋グループ付き字幕削除をホストUndo一回で復元。先行手編集を保持|自動Undoの呼び出し、失敗時の保護された復元、再起動後・任意データの復元は未成立|

## 環境と方法

既存の隔離AviUtl2環境・同版SDK（2010900）を使用。利用者によるプラグインの信頼確認後に実行した。修正版DLL SHA256は `8F10BC9EE800142DA436BBA43817FE74E4174760747159ABD3472D9EB8142EB2`。ソースは [transaction-probe.cpp](evidence/editor-sdk/transaction-probe.cpp)、ビルドは [build-transaction.cmd](evidence/editor-sdk/build-transaction.cmd)。

検証コピーは映像3・字幕3・画像1・音声1の8オブジェクト。第2映像と第2字幕が同一グループ。前回の素材・元仕様書は変更していない。

### 検証コードの不具合と修正

初回コードは参照専用セクションで `get_project_file` を呼び、SDKが禁止する呼び出しになっていた。AviUtl2は例外（0xE06D7363、callEditSection）を表示し、バックアップ後終了した。生成・削除はまだ実行されていなかった。これは検証コード側の不具合であり、製品方式が失敗した証拠とは扱わない。

該当呼び出しを編集セクションへ移し再ビルドした。再起動時のホスト復元操作で8オブジェクトと先行変更X=5を回復した。旧ログとバックアップは `transaction-preflight-error.jsonl`、`transaction-preflight-backup.aup2` に保持した。故意の失敗注入ではなく、この事故を一般的な自動復旧の合格にも数えない。

## 手編集排他と複合操作

修正版セッション `9196-111359671` で字幕CのXを5→6へ手変更した。明示保存せず待機画面を開き、ドラッグとCtrl+Zを試した。さらに待機画面のApplyで、画像を空きレイヤーへ生成し、第2字幕を削除した。両操作は一つの編集セクション内で実施した。同セクション内で作ったオブジェクトを削除する禁止操作は使っていない。

待機画面は73,313ms継続し、72回の状態取得を記録した。適用前・適用後の取得値は各々一定で、途中の更新通知は複合操作の1件のみ。適用後にもドラッグを試し、閉じた後のクリックでも移動の持ち越しは観測しなかった。要求時刻は適用前ドラッグ06:39:19.895Z、Undo06:39:29.230Z、適用後ドラッグ06:40:13.102Z。これらは要求時刻で、OS配送時刻の計測ではない。

待機画面を閉じて保存後、ホストのCtrl+Z一回を実行し、再保存した。保存aup2の全数値オブジェクト節・効果節を比較し、字幕CのX=6以外は初期コピーと一致した。focusのみ比較から除外した。字幕Bと映像Bの `group=1` も復元された。つまり生成物の除去、削除物の復帰、グループ、先行手編集の保持をこの検証コピーで確認した。

ただしUndoは待機画面を閉じた後のホストUI操作である。自動復元をロック解除前に呼べることや、途中失敗・別操作の割込みに対する保証にはならない。

## 保存状態と出力開始

保存通知ごとの識別値と状態を記憶し、実際の保存先aup2に記載された識別値で選ぶ方式を試した。最新通知だけを使う前回方式から変更しており、今回のディスク識別値 `9196-111359671-2` でArmが成立した。バックアップとの競合条件そのものは修正版では再現していない。

専用出力コールバック開始時（tick 111929671、edit state=2）にSDKで再取得すると `UNAVAILABLE` となった。`cannot prove exact armed state` と記録して停止し、指定した `transaction-clean.cctx` は作られなかった。照合を省略して成功扱いにはしていない。

この結果から、保存通知・識別値・出力入口でのSDK再取得だけでは、正常出力できる競合防止経路がまだ作れていない。通知対象外データ、通知遅延、全状態、標準出力入口は引き続き未保証。出力用に固定した状態を渡す経路などの技術検討が必要で、ここでは方式を採用決定していない。

## 証跡・終了状態

[transaction-result.json](evidence/editor-sdk/transaction-result.json) と [check-transaction.py](evidence/editor-sdk/check-transaction.py) に照合結果を記録した。10確認項目が成立したが、失敗・未成立を正しく観測したことも含むため、要件全体の合格ではない。ログは `transaction.jsonl`、保存比較用データは `transaction-before.aup2`、`transaction-applied.aup2`、`transaction-after-undo.aup2`。

検証コピーは生成・削除をUndoした8オブジェクト、字幕CのX=6を保存した状態で保持した。先行変更を保持する証跡として残しており、初期X=0へ戻したとは扱わない。復旧待ち処理や実行中出力はない。製品の模擬失敗ケースは追加注入していない。

ADR-0003の保護条件を緩めず、残る作業は「保護中に復元を呼ぶ経路」と「保存した全状態を固定して出力へ渡す経路」の技術的成立確認。複数シーンや外部プラグイン固有状態への一般化も今回の結果からは保証しない。

## 2026-09-18 保存コピーの固定とSDK経路の追加確認

`check-frozen-file.py` をWindowsのPythonで実行し、既存aup2の使い捨てコピーについて8項目が成立した。読み取り専用ハンドルを共有読み取りのみで保持すると、別プロセスの読み取りは成功し、書き込みとDELETE権限取得は共有違反（Win32 error 32）で拒否された。先行する書き込みハンドルがある場合も保護開始は拒否された。解除後は書き込み権限取得が成功した。内容の破壊や実ファイル削除は行わず、ハンドル取得だけを試験した。全節を含むファイルコピーのバイト一致も確認した。

結果は [frozen-file-result.json](evidence/editor-sdk/frozen-file-result.json)、再現スクリプトは [check-frozen-file.py](evidence/editor-sdk/check-frozen-file.py)。これはファイル保護の検証であり、保存完了・ホスト内部状態の固定・動画出力・自動復元の合格ではない。

次に別プロセスで同じ共有制約を保持し、AviUtl2の「開く」から `frozen.aup2` を読み込んだ。映像3・字幕3・画像1・音声1のタイムラインとプレビュー、字幕CのX=6を確認した。保護中にX=7へ変更すると未保存表示になり、メモリ上の編集が成立した。ディスクのSHA256は元コピーの `D2231369D203AC961D811FE59E7CFA5A83C5E0D31C5561CCA40211A175295B3F` のままだった。よって「ファイルを固定すれば出力する内部状態も固定できる」という推論は成立しない。X=7はUndoでX=6に戻し、ロックプロセスを解除・正常終了させた後にコピーを保存した。元のtransaction-fixtureや元仕様を変更していない。実行用保持スクリプトは `hold-frozen-file.py`（今回の一時パス専用）、ホストログは `frozen-host.jsonl`。

ローカルの同版SDK `plugin2.h` も再確認した。734–741行には出力中等はcall_read_sectionが失敗すると記載され、前回のUNAVAILABLEと整合する。354–374行ではシーン名・解像度・フレームレート・サンプリングレートの操作がUndo非対応。684行の「編集オブジェクトはまとめてUndo登録」をシーン操作まで一般化しない。PROJECT_FILE（858–892行）はプラグイン固有データの取得・設定とパス取得で、プロジェクト全体の保存・読み戻しAPIではない。公開ヘッダー内に全体save/load/undoの呼び出し口は見つからなかった。非公開コマンドの推測呼び出しは行っていない。

**現在の判断**：保存ファイル自体を保護してホストに読ませる部品は成立。ただし素材ファイルや外部プラグイン状態はこのロックで保護されず、読込後の内部状態も編集できる。専用の出力経路への接続と保護中の全体復元は未成立。単にロック対象を増やすだけでは解決しない。公開SDKでの制約と残る要件を保持し、仕様・採用判断は変更しない。

後処理の保存ファイルも `frozen-after-undo.aup2` として保持し、初期コピーと全オブジェクト・効果節が一致した（focusを除外）。結果は frozen-host-result.json。


## 2026-09-18 標準バッチによる出力状態の分離

公開SDKの出力入口で状態を再取得する案から、ホストが登録時のプロジェクトを保持する標準バッチ経路へ検証対象を変更した。同梱 `aviutl2.txt` の694–702行にバッチ登録、1296行に開始時プロジェクトを終了時に復元する説明がある。追加プラグインや非公開コマンドは利用せず、公開UIで実施した。

1. 保存済みの検証コピー（字幕CのX=6）からAVI出力ダイアログのバッチ登録を実行。
2. `data/Batch/Batch_2026-09-18_16-04-40-606.queued.aup2` が作られた。証跡 `batch-registered.aup2` の全オブジェクト・効果節は登録前の保存状態と一致（focusを除外）。
3. 登録後に字幕CをX=70へ変更。バッチ開始時にホストの保存確認が出たため「はい」を選び、その変更を元の検証コピーへ保存した。この分岐では未保存変更の保持試験にはならない。`batch-current-x70.aup2` はX以外の全オブジェクト情報が元と一致した。
4. バッチ出力中のホスト表示はX=6となり、正常終了後にX=70へ復帰した。今回の短い出力中に手編集を注入する試験は行っていない。
5. 出力ファイル `batch-frozen-x6` は拡張子なしで作られたが、AVI（RGB24+PCM16）、320×180、30fps、12秒・360フレーム。全デコード成功。最終フレームを抽出し、字幕がX=70の右寄り位置ではなく登録時の中央付近に描画されることを目視確認した。全フレームの参照出力との画素一致は未実施。
6. 試験後、手動でX=6へ戻して保存し、`batch-final.aup2` の全オブジェクト・効果節が初期状態と一致することを確認した。バッチ一覧は空、出力処理も完了している。

結果は [batch-result.json](evidence/editor-sdk/batch-result.json)、再現比較は [check-batch.py](evidence/editor-sdk/check-batch.py)、出力確認画像は [batch-last-x6.png](evidence/editor-sdk/batch-last-x6.png)。6項目の比較が成立した。動画本体は隔離ランタイムに保持し、SHA256とサイズを結果JSONに記録した。ログは `batch-host.jsonl`。

**前進した点**：登録済みのプロジェクト状態を後続編集から分離して動画出力するホスト側の経路が、この標準AVI・単一シーンの検証で成立した。前回の「出力経路全体が未成立」という状態から、標準UIバッチ経路の限定成立へ更新する。

**未解決の境界**：明示保存→バッチ登録の間の保護、登録データと明示保存の機械的な同一性判定、アプリからのバッチ登録・開始・完了通知、外部素材やプラグイン設定の固定は未成立。標準説明にも一部出力プラグインではバッチ側に設定が保持されないとあるため、このAVI結果を他出力方式へ一般化しない。

### 排他中の自動復元についての到達点

同版 `plugin2.h` を再検索したが、全体のload/save/undoを開始する公開関数は確認できなかった。取得できるトラックバーのgroup情報は、以前消失したオブジェクト所属のgroupとは別物であり、グループ復元の代用にはできない。シーン設定はUndo対象外で、aliasによる再生成ではgroup欠落の反例が既にある。

よって、今回も排他中の一般的な自動復元は実行できたとは報告しない。標準バッチ内部の正常終了復帰は確認できたが、それを任意の編集途中失敗の復元に呼び出す公開入口は見つかっていない。既に失敗が分かっているalias再生成や、正常Undoの反復は追加していない。次に必要なのはこの入口の確立（公開APIの追加・確認、またはサポートされる別の制御方式の検討）であり、その前に故意の失敗ケースを増やしても合格条件を満たせない。ADR-0003/0005と仕様は変更しない。

## 2026-09-18 公開制御APIの棚卸しと次の判定条件

検証に使用した `plugin2.h` の関数ポインタ宣言132件を [sdk-control-inventory.json](evidence/editor-sdk/sdk-control-inventory.json) に記録した。再現用は `inventory-control-api.py`。名前検索だけを不可能性の証明とはせず、プロジェクト・編集ハンドル・ホストテーブルの注記と合わせて確認した。

- `register_project_save_handler` は保存直前、`register_project_load_handler` はロード直後の通知登録。操作開始APIではない。
- `rendering_scene_video/audio` は非同期タスクを追加する。`wait_rendering_task` は参照・編集ロック内ではデッドロックする可能性が明記されている。長時間編集ロックを保持して同期待ちする実験は行わない。
- `FLAG_PROJECT_CONFIG` と出力プラグインの `func_save_project_config` / `func_load_project_config` は出力設定保持の公開入口。全体プロジェクトの保存・再読込みやバッチ開始の代わりではない。実装・動作確認は未実施。

公式配布物の再確認は [公開制御経路の調査](editor-public-control-research.md) にまとめる。今回、既存の正常Undo/バッチ出力や失敗注入は繰り返していない。製品実装・原仕様変更はない。

### 次の最小検証（採用決定ではない）

候補はSDK編集＋公開UI操作の併用。標準バッチの出力状態分離は既に確認したため、次は入口の保護を先に判定する。

1. 操作用の保護を開始した状態で、制御側だけが公開UIの「別名保存」「開く」「バッチ登録・開始」を実行できるか。前回のモーダルはホスト自体のUI入力を無効化するため、それを閉じて操作するだけでは排他の成立に数えない。全PCの入力を遮断したり、非公開コマンドIDを推測して実行したりする方式はこの検証の候補にしない。
2. この入口が成立した場合のみ、未保存の先行変更を含む全体退避→正常な編集→保護中の再読込みを試す。全オブジェクト・グループ・シーン等と先行変更の保持を照合する。正常往復が成立する前に故意の失敗ケースを追加しない。
3. 出力では保護中の保存→登録→内容照合を連続実行し、その後にバッチを開始する。保存済みの状態と登録データが異なる場合には出力しない。専用出力設定の保存・再読込みと完了・中止通知の確認は別の判定項目にする。

最初の入口を保護したまま呼べなければ、以後の復元・出力試験は進めず、公式にサポートされた制御APIの追加・拡張方法の確認が必要という技術的な停止条件にする。UI自動操作を正式採用したり、A37/A41/A84の排他・復元要件を緩和したりする決定ではない。

## 2026-09-18 モーダル排他中の公開UI制御の入口確認

既存Transaction Probe v5の待機画面を開き、排他を解除せずに標準の保存・読込みを開始できるか確認した。Ctrl+Shift+S（07:21:04.618 UTC）とCtrl+O（07:21:11.624 UTC）を自動入力したが、どちらのダイアログも開かなかった。UIAではホストがdisabled、待機画面がフォーカス対象だった。対照として、待機前のCtrl+Oによるfixture読込みと、解除後のCtrl+Shift+Sによる保存ダイアログ表示は成立した。後者はキャンセルし、保存していない。

最新のmodal_begin〜modal_endは32,454ms、32サンプル。全サンプルと終了時の取得値が開始時と一致し、この区間にsave/load/update/compoundイベントはなかった。これは限定されたaliasスナップショットの比較で、オブジェクト所属グループや全シーンを含む完全状態の証明ではない。fixtureの前後SHA256はD2231369D203AC961D811FE59E7CFA5A83C5E0D31C5561CCA40211A175295B3Fで一致した。

証跡は [modal-control-result.json](evidence/editor-sdk/modal-control-result.json)、累積ログは modal-control.jsonl、最後のモーダルだけを集計するスクリプトは check-modal-control.py。UI観察とログから再計算する確認を分けて記録した。Apply、追加の模擬失敗、出力は実行していない。ダイアログは閉じ、通常の編集画面へ復帰した。

**判断**：モーダル待機画面＋キーボード入力による標準UI制御案は入口条件を満たさない。排他解除後の保存・Undoを、排他中の自動復元の成立とは扱わない。UIA Invokeや別の制御方式全般が不可能と証明したわけではない。

**次の技術判断**：手入力を防ぎながらコントローラーが全体保存・再読込み・バッチ登録を開始できる、サポートされた制御入口が必要。公開SDK調査でも該当入口は未発見であり、同じモーダル方式の反復試験はここで終了する。別方式を採る場合は、まず「保護中の保存→再読込み」の正常往復だけを確認し、成立後に復元・出力の結合検証へ進む。出力設定保持用FLAG_PROJECT_CONFIGだけを追加しても、この制御入口の問題は解決しない。排他中の一般的自動復元と明示保存→登録の競合防止は未成立のまま、仕様・採用判断を確定しない。
