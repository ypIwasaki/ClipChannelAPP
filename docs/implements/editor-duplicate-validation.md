# 字幕の複製・独立編集・配置保存の限定検証

2026-09-18。A105の限定検証許可と継続指示に基づき、分離済みAviUtl2 2.1.9の標準テキストで実測。既存のsubtitle-roundtrip.aup2を別名へコピーし、既存成果物は変更していない。追加プラグイン・アプリ側連携コードは使用していない。

## 実施結果

1. Layer1の日本語2行字幕を、右クリックメニューの「複製」でLayer2へ複製。同じ時刻・本文を持つ別オブジェクトが表示された。
2. 複製側だけ本文を「複製側だけ変更：ハイライト」へ変更した。元の2行は残った。
3. 複製側を後の時刻へドラッグし、保存。保存ファイルでも本文と配置が別々に記録されていることを確認した。
4. 移動をCtrl+Zで戻すと元と同じ時刻へ戻り、Ctrl+Shift+Zで移動後へ戻った。複製側の変更本文は画面上で維持された。この履歴確認は画面観測であり、各中間状態を保存して全項目比較したものではない。
5. 通常終了、新しいプロセスで同ファイルを再読込み。3.30秒で元の2行、プレビューのシークバーから6.30秒へ移動すると変更済みの1行が表示された。確認後、通常終了した。

| 対象 | 保存されたlayer | 保存されたframe | 本文 |
| --- | --- | --- | --- |
| 元 | 0 | 59,139 | 検証用字幕：変幻リメ（改行）2行目：保存・再読込み確認 |
| 複製 | 1 | 158,238 | 複製側だけ変更：ハイライト |

[保存ファイル](evidence/editor-normal/subtitle-duplicate.aup2)は2,165 bytes、SHA256 `6E80113308C85B88BAEAAA4193A0F4BAB1D6458FE9432C8F85B8F9EBC4368600`。検証時の絶対パスを含む。元仕様のSHA256は `CFC9E65594F7B72470DDDEF9EFA06776DA0FA28A2B884B57F6445082F8F83DC7` から不変。

## 観測上の注意

タイムラインの目盛りをクリックしても今回は再生位置が変わらず、プレビューのシークバーで変更できた。画面上の入力位置と実際に変わった時刻を照合する必要がある。固定座標による今回の手順を製品連携方式として採用したものではない。

## 判定

A71のうち標準字幕単体の複製後の本文独立性と保存を確認。E2の部分確認を拡張し、E3は複製字幕1個の移動Undo/Redoを画面上で部分確認した。

映像と対応字幕・効果の一括複製、補助素材の選択複製、リップル移動、複合操作全体のUndo/Redo、手編集後の履歴境界、アプリ側保存世代は未検証。E4の排他・出力、E5の未保存状態を含む復元も未達。模擬失敗枠は消化していない。仕様変更・確定、本体実装、Issue分割には進んでいない。

## 2026-09-18追試：標準描画の配置独立性

前回成果物をsubtitle-placement.aup2へコピーし、複製字幕のプレビューをドラッグして画面位置を変更した。保存された標準描画は、元がX=0.00 / Y=0.00、複製側がX=-294.79 / Y=251.91。両者の本文・frame（59,139と158,238）は維持されていた。

同じプロセスで「開く」から保存ファイルを再読込みし、6.30秒で変更した配置、3.26秒で元の2行字幕の従来配置を確認。通常終了した。今回の再読込みはプロセス再起動試験ではない。

[配置検証ファイル](evidence/editor-normal/subtitle-placement.aup2)は2,171 bytes、SHA256 `3DFAFF36721813C403756C7E1B15941C6B8A13858042ACC7403B61B2B3B6C628`。

標準字幕単体の本文に加えて、標準描画のXY配置も複製後に独立して変更・保存できることを確認した。任意の効果やプラグイン固有状態へ一般化しない。映像・字幕・補助素材の一括操作、アプリ連携、排他、復元の未検証判定は変更しない。
